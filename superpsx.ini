# SuperPSX Configuration File
#
# Boot mode (choose one):
#   boot = bios          Boot directly into the BIOS shell (no ROM needed)
#   rom  = <path>        Load a PSX executable or disc image
#
# BIOS image path (default: bios/SCPH1001.BIN)
#   bios = bios/SCPH1001.BIN
#
# Subsystem enable/disable (default: enabled)
#   audio       = enabled | disabled
#   controllers = enabled | disabled
#
# Region (affects VBlank frame timing)
#   region = ntsc | pal   (default: ntsc)
#
# Examples:
#   rom = isos/MortalKombat2JP/MortalKombat2JP.cue
#   rom = tests/gpu/triangle/triangle.exe
#   boot = bios

rom = isos/MortalKombat2JP/MortalKombat2JP.cue

# GTE VU0 macro mode (default: 1 = enabled for games)
# Set to 0 for exact GTE flag behavior (GTE test suite)
# gte_vu0 = 0
